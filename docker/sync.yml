version: '3.2'

services:

  service:
    image: digigladd/helloan/sync:1.0
    environment:
      - APPLICATION_SECRET=jenesuispasunheros
      - HOSTNAME=service
    networks:
      - default
    ports:
      - "127.0.0.1:9002:9000"

networks:
  default:
    external:
      name: digigladd
